export type project = {
    title: string;
    description: string;
    tech: string[];
    src: string;
    image: string;
    featured: boolean;
    videoUrl?: string;
    githubUrl?: string;
    liveUrl?: string;
    color: string;
};